"""
Common search utilities.
"""

import pacai.core.search

def unit_cost_function(node: pacai.core.search.SearchNode) -> int:
    """
    One of the most simple search functions,
    always return 1 (a single unit).
    """

    return 1
